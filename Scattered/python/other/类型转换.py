'''

int(x [,base]) ⇒ x转换十进制整数
long(x [,base]) ⇒ x转换十进制整数
float(x) ⇒ x转换浮点数
str(object) ⇒ 转换字符串
repr(object) ⇒ 转换表达式字符串
eval(str) ⇒ 用计算字符串效Python表达式,并返象
tuple(seq) ⇒ 序列seq转换元组
list(seq) ⇒ 序列seq转换列表
chr(x ) ⇒ 整数转换字符
unichr(x ) ⇒ 整数转换Unicode字符
ord(x ) ⇒ 字符转换整数值
hex(x ) ⇒ 整数转换十六进制字符串
oct(x ) ⇒ 整数转换八进制字符串

'''